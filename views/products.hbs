<div class="flex flex-col gap-xl">
    <h1>{{title}}</h1>
        <div class="flex flex-col gap-lg">
    {{>button type="button" text="Tilføj personale" variant="secondary"}}

{{#each products}}

	<div class="button-box">
		<div class="button-box__title">
             {{name}}
        </div>
		<img src="../assets/icons/edit.svg" alt="">
	</div>

{{/each}}

    {{>footerNav}}
</div>