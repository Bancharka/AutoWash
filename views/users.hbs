<div class="flex flex-col gap-xl"> 
    <h1>{{title}}</h1>
        <div class="flex flex-col gap-lg">

            {{#each users}}

                <a href="/users/{{id}}" class="button-box block">
                    <div class="button-box__title">
                        {{firstName}} {{lastName}} 
                    </div>
                    <img src="../assets/icons/edit.svg" alt="">
                </a>

            {{/each}}

            {{>footerNav}}
        </div>


</div>