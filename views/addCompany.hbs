<div class="flex flex-col gap-xl">
    <h1>{{title}}</h1>
    <div class="flex flex-col gap-lg">
        <form class="flex flex-col gap-lg" action="/add-company" method="POST">
            {{>labeledInput type="text" id="name" name="name" label="Indtast firmanavn" required=true}}
            <div class="grid grid-cols-2 gap-md">
                {{> button href="/stations" text="Annuller" variant="outline"}}
                {{> button type="submit" text="Tilføj"}}
            </div>
        </form>

        <div class="flex flex-col gap-md">
            {{>label text="Eksisterende firmaer"}}
            <div class="list-items"> 

                {{#each companies}}
                        {{ name }} <br>
                {{/each}}
            </div>
        </div>
        
        {{> footerNav}}
    </div>
</div>