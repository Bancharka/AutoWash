<div class="flex flex-col gap-xl">
    <h1>{{title}}</h1>
    <div class="flex flex-col gap-lg">
        {{>button href="/new-cleaning" text="Tilføj rengøring" variant="secondary"}}
        <div class="flex flex-col gap-md">
            {{>separator text="Fuldført"}}
            <div class="flex flex-col gap-sm">
                {{#each logs}}
                    {{>itemCard href="#" title=(concat stations.companies.name ", " stations.address) body=(formatDate createdAt)}}
                {{/each}}
            </div>
        </div>
    </div>
</div>