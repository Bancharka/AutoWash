<div class="dashboard-display">
  <div class="dashboard-display__content">
    <div class="dashboard-display__title">{{title}}</div>
    <div class="dashboard-display__date">{{date}}</div>
  </div>
  <div class="dashboard-display__link">
    {{#if variant}}
      <img src="/assets/icons/{{variant}}.svg" alt="{{variant}} icon" class="dashboard-display__icon">
    {{else}}
      ›
    {{/if}}
</div>