{{#unless isHidden}}
  <header class="header">
    <div>
      {{#if backUrl}}
        <a href="{{backUrl}}" class="header__item">
          <img src="/assets/icons/backArrowsvg.svg" alt="" />
        </a>
      {{/if}}
    </div>
    <div>
      {{#if logout}}
        <a href="/logout" class="header__item">
          <img src="/assets/icons/logout.svg" alt="" />
        </a>
      {{/if}}
    </div>
  </header>
{{/unless}}